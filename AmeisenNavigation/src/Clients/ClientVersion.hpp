#pragma once

/**
 * @enum CLIENT_VERSION
 * Represents different versions of the client.
 */
enum class CLIENT_VERSION : char
{
	V335A, /**< Client version 3.3.5a. */
	V548,  /**< Client version 5.4.8. */
};
